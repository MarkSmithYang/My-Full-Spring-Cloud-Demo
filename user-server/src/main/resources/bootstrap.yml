#设置服务端口
server:
  port: 9097

#eureka服务注册中心的配置
eureka:
  client:
    service-url:
      defaultZone: http://eureka.didispace.com/eureka/
#spring的配置
spring:
  application:
    name: user-server
  profiles:
    active: dev
  #mongodb配置
  data:
    mongodb:
      database: app
      host: localhost